name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build-and-release:
    name: Build and Release Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract version from tag
        id: meta
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "tag_version=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          tags: |
            aaif-mcp-server:${{ steps.meta.outputs.version }}
            aaif-mcp-server:latest
          load: true

      - name: Test Docker image
        run: |
          docker run --rm -e AAIF_MCP_TRANSPORT=stdio aaif-mcp-server:${{ steps.meta.outputs.version }} python -m aaif_mcp_server --version || true

      # Uncomment below to push to a container registry (requires secrets configuration)
      # - name: Log in to GitHub Container Registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ github.actor }}
      #     password: ${{ secrets.GITHUB_TOKEN }}
      #
      # - name: Push Docker image to registry
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     file: ./Dockerfile
      #     push: true
      #     tags: |
      #       ghcr.io/${{ github.repository }}:${{ steps.meta.outputs.version }}
      #       ghcr.io/${{ github.repository }}:latest

      # Uncomment below to create a GitHub Release
      # - name: Create GitHub Release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     tag_name: ${{ steps.meta.outputs.tag_version }}
      #     release_name: Release ${{ steps.meta.outputs.version }}
      #     draft: false
      #     prerelease: false
