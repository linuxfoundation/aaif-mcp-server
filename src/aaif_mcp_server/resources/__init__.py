"""MCP Resources — read-only data endpoints exposed via resource:// URIs.

Resources let Claude (or any MCP client) access foundation data without
calling a tool. They're ideal for context-loading: the client can read
a member profile or checklist before deciding which tools to call.
"""
